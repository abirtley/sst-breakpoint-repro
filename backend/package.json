{
  "name": "@breakpoint-repro/backend",
  "version": "0.0.1",
  "scripts": {
    "script": "esr",
    "test": "jest --keep=true"
  },
  "dependencies": {
  },
  "devDependencies": {
    "esbuild": "^0.14.25",
    "esbuild-jest": "^0.5.0",
    "esbuild-runner": "^2.2.1",
    "jest": "^27.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
